{% extends "layout.html.twig" %}
{% block contenu %}

        <div class="container">
            <div class="">
        <h4>Produits disponibles</h4>
    {% for produit in data if data is not empty %}
           

        <div class="col-lg-6">

            <caption>{{produit.libelle_produits}}</caption>
        <table class="table table-striped">

        <tbody>

            <tr>
                 <td>{{ produit.libelle_typeProduits }}</td>
                    <td>prix :{{ produit.prix_produits }}</td>

                    <td>

                        <a href="{{ path('panier.add',{idProduit:produit.id_produits}) }}">ajouter au panier</a> <br>
                        <a href="#"> détails sur le produit</a>
                    </td>
            </tr>

            </tbody>
            </table>
            </div>
            
        {% endfor %}
        </div >
        <div class="">
        <h4>Panier</h4>
                {% for panier in panier if panier is not empty %}

                

                    <div class="col-lg-4">

                            <caption>{{panier.libelle_produits}}</caption>
                            <table class="table table-striped">
                                <tbody>
                                <tr>

                                    <td>
                                        prix :{{ panier.prix_panier }} <br>
                                        Quantite :{{ panier.quantite_panier }} <br>
                                        Date de fin de vente : {{ panier.heure_fin_vente|date('d/m/Y H:m:s' )}}
                                    </td>
                                    <td>
                                        <a href="{{path('panier.deleteProduit', {id: panier.id_panier})}}">Suppression</a> <br>
                                    </td>

                                </tr>
                                </tbody>
                            </table>
                       </div>
                    

                {% endfor %}
            </div>
            </div>
      

{% endblock %}