{% extends "layout.html.twig" %}
{% block contenu %}

        <div class="container">
            <div class="">
        <h4>Produits disponibles</h4>
    {% for produit in data if data is not empty %}
           

        <div class="col-lg-6">

            <caption>{{produit.libelle_produits}}</caption>
        <table class="table table-striped">

        <tbody>

            <tr>
                 <td>{{ produit.libelle_typeProduits }}</td>
                    <td>prix :{{ produit.prix_produits }}<br>
                stock :{{ produit.stockProduit }}</td>

                    <td>

                        <a href="#">ajouter au panier</a> <br>
                        <a href="{{path('produit.detail', {idProduit: produit.id_produits})}}"> détails sur le produit</a>
                    </td>
            </tr>

            </tbody>
            </table>
            </div>
            
        {% endfor %}
        </div >

            </div>
      

{% endblock %}